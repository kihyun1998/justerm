{
  "name": "@justerm/renderer-proofs",
  "private": true,
  "type": "module",
  "description": "Headless runner for justerm-renderer's demo/*.html GL proofs. Not the published wasm package — that is produced by wasm-pack into pkg/.",
  "scripts": {
    "build:wasm": "wasm-pack build --target web --dev --out-dir pkg",
    "serve": "node scripts/serve.mjs",
    "test:proofs": "pnpm build:wasm && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}

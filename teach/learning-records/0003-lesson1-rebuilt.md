# 0003 — 레슨 1 통째 재작성 (틀린 프레임을 근본에서 교체)

날짜: 2026-07-11

## 계기
사용자가 레슨 1 §1 오프닝 "justerm 은 순수 터미널 엔진이다. 딱 이 한 줄이 전부다" 를 지적.
→ 그 줄 하나가 아니라 **레슨 전체가 "justerm = 엔진 하나"라는 pre-pivot 프레임 위에** 서 있었고,
§7 에서야 뒤집는 거꾸로 구조였다. 사용자: "그 줄만 할 게 아니라 1번 teach 다시 만들어."

## 무엇이 틀렸었나
- 옛 레슨은 "justerm(패밀리) = 순수 엔진" 을 전제로 깔고(= 방금 repo 문서에서 고친 그 conflation),
  피벗/패밀리를 §7 각주로 취급. 리뷰어 멘탈모델의 골격이 거꾸로.

## 재작성 원칙 (올바른 골격 처음부터)
1. **1번부터 진실**: `justerm` = 패밀리(우산). `justerm-core` 만 순수 엔진. 패밀리 전체는 풀스택.
2. **패밀리 지도(5크레이트) + 데이터 흐름**을 §2–3 앞단으로 (예전 "레슨0 family map" 제안을 흡수).
3. **핵심 win 재정의**: 경계는 크레이트마다 다르다 → 리뷰 첫 질문 = **"어느 크레이트냐"**, 그다음 그 경계.
4. core 리트머스 4질문은 **core 한정**으로 스코프 명시. renderer 는 그리는 게 임무(theme-agnostic).
5. 우회(#297) 유지. 피벗(ADR-0018)과 "문서가 아직 beamterm 이라 하는 이유(#274)"를 §6 로 — 방금
   사용자와 실제로 겪은 사건을 교보재로.
6. 퀴즈 5문항을 "어느 크레이트냐/경계/우회" 중심으로 교체(Q1 = "core 가 이제 그리나?" → 틀림).

## 상태
- `lessons/0001-boundary-is-identity.html` 통째 덮어씀(파일명·nav 유지).
- 용어집엔 이미 family·renderer·web·decode·피벗 엔트리 있음(record 0002) → 일관.
- 다음(레슨 2) 후보 확정: **"어느 크레이트냐 — core vs 소비처 라우팅"(ADR-0017)**. nav 에도 반영.

## 교훈(나 자신에게)
repo 문서를 고쳤으면 **같은 지식을 담은 teach 자료도 동시에 동기화**해야 divergence 안 생긴다
(CLAUDE.md "우회 금지/근본에서 고쳐라"의 교육자료 판). 사용자가 그 divergence 를 잡아줬다.
